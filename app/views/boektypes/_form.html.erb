<%= form_for(@boektype) do |f| %>
  <% if @boektype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boektype.errors.count, "error") %> prohibited this boektype from being saved:</h2>

      <ul>
      <% @boektype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :naam %><br>
    <%= f.text_field :naam %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
